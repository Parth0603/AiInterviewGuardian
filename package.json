{
  "name": "ai-interview-guardian-root",
  "version": "1.0.0",
  "description": "Root orchestrator for AI Interview Guardian fullstack application",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps"
  }
}
